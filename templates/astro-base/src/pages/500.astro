---
import Registry from '../Registry.astro';
import config from '../lander-config.json';

const errorPages = config.errorPages || {};
const custom500 = errorPages['500'];
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>500 - Internal Server Error</title>
    <style>
      .lander-error-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 100vh;
        font-family: system-ui, -apple-system, sans-serif;
        text-align: center;
        padding: 2rem;
      }
      .lander-error-title {
        font-size: 4rem;
        margin: 0;
        color: #333;
      }
      .lander-error-message {
        font-size: 1.5rem;
        color: #666;
      }
      .lander-error-back {
        margin-top: 2rem;
        padding: 0.5rem 1rem;
        background: #007bff;
        color: white;
        text-decoration: none;
        border-radius: 4px;
      }
    </style>
  </head>
  <body>
    {custom500 ? (
      <Registry component={custom500.component} props={custom500.props} />
    ) : (
      <div class="lander-error-container">
        <h1 class="lander-error-title">500</h1>
        <p class="lander-error-message">Internal Server Error</p>
        <a href="/" class="lander-error-back">Go back home</a>
      </div>
    )}
    <script>
      import '../registry-manifest';
    </script>
  </body>
</html>
